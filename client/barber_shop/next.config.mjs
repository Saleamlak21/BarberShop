/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['educationcontent.schwab.com','drive.google.com','www.google.com'], // Add 'www.google.com' or any external domains you need
      },
};

export default nextConfig;
